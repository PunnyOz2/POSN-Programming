#include <stdio.h>
int main (){
    int First;
    int Days;
    int week;
    printf("Enter the first Monday of the month: ");
        scanf("%d",First);
    printf("Enter the number of days in that month: ");
        scanf("%d",Days);
    for(week=7,)
}
